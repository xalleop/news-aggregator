# 🚀 Быстрый старт

## 1. Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите "+" → "New repository"
3. Имя: `news-aggregator`
4. Тип: **Public** (важно!)
5. ✅ "Add a README file"
6. Нажмите "Create repository"

## 2. Загрузите файлы

**Простой способ (веб-интерфейс):**

1. Нажмите "Add file" → "Upload files"
2. Перетащите все файлы из архива
3. Нажмите "Commit changes"

**Через git:**

```bash
git clone https://github.com/ваш-username/news-aggregator.git
cd news-aggregator
# Скопируйте все файлы сюда
git add .
git commit -m "Initial setup"
git push
```

## 3. Настройте расписание (опционально)

Откройте `.github/workflows/news-monitor.yml`:

```yaml
schedule:
  - cron: '0 6 * * *'   # 09:00 МСК - утро
  - cron: '0 12 * * *'  # 15:00 МСК - день  
  - cron: '0 18 * * *'  # 21:00 МСК - вечер
```

Измените по желанию. Время в UTC (Москва = UTC+3).

## 4. Настройте фиды (опционально)

Откройте `feeds.yaml`:

**Отключить источник:**
```yaml
  Медуза:
    enabled: false  # Поставьте false
```

**Изменить ключевые слова:**
```yaml
filters:
  keywords:
    - "ваша тема"
    - "другая тема"
```

## 5. Протестируйте

**На GitHub:**

1. Откройте вкладку "Actions"
2. Выберите "News Aggregator"
3. Нажмите "Run workflow" → "Run workflow"
4. Подождите 2-3 минуты
5. ✅ Зеленая галочка = работает!

**Посмотрите результаты:**

1. Скачайте Artifacts (zip-архив)
2. ИЛИ откройте `reports/latest.txt` в репозитории

**Локально (на компьютере):**

```bash
pip install -r requirements.txt
python news_agent.py
cat reports/latest.txt
```

## 6. Анализ через Claude

1. Скачайте `reports/raw_articles_[дата].json`
2. Откройте claude.ai
3. Загрузите файлы:
   - `raw_articles_[дата].json`
   - `criteria.yaml`
4. Напишите:

```
Проанализируй эти новости используя критерии из criteria.yaml.
Сгруппируй по темам, оцени значимость, создай отчет.
```

5. Сохраните результат! ✅

## Готово! 🎉

Агент будет автоматически собирать новости 3 раза в день.

---

**Проблемы?** Читайте [полную инструкцию](README.md) или создайте Issue.
